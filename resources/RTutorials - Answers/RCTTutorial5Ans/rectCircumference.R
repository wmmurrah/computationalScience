rectCircumference = function(l, w) {
# RECTCIRCUMFERENCE Circumference of a rectangle
#    rectCircumference(l, w) returns the circumference of a 
#    rectangle with length l and width w.
   return(2 * l + 2 * w)
}
